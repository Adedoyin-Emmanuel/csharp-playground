namespace Csharp_Playground.Playground.CustomLinkedList;

public class CustomNode
{
   /*
    * A node represents a single element in a linked list
    * A node has 2 properties. A value and a next which points to the
    * next node in the linked list.
    */

   private int Value;
   private CustomNode Next;
}
